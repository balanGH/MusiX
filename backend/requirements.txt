# MusiX audio-studio service
#
# Optional: MusiX runs fully without the separation dependencies below.
# Install them only if you need stem separation (spec §18–§19).

# ============================================================
# Web service
# ============================================================

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9        # multipart/form-data uploads

# ============================================================
# Audio / metadata
# ============================================================

mutagen>=1.47,<2.0             # ID3 USLT lyrics frames
yt-dlp                         # Audio/video downloads

# FFmpeg is a system dependency, not a Python package.
#
# macOS:
#   brew install ffmpeg
#
# Debian/Ubuntu:
#   sudo apt install ffmpeg
#
# Windows:
#   https://ffmpeg.org/download.html
#   Then add FFmpeg to PATH.

# ============================================================
# Stem separation — optional
# ============================================================
#
# Demucs 4.x provides htdemucs / htdemucs_6s.
# NumPy is capped below 2.x for compatibility with Demucs.

demucs>=4.0.1,<5.0
torch>=2.1,<3.0
torchaudio>=2.1,<3.0
numpy>=1.24,<2.0
