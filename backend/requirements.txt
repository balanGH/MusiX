# MusiX audio-studio service.
#
# Optional: MusiX runs fully without any of this. Install it only if you want
# stem separation (spec §18–§19).
#
# The previous version of this file pinned `demucs==3.0` (which does not exist
# on PyPI) alongside `torch==2.1.2`, listed torchvision for a project with no
# vision model, and had FastAPI itself commented out — so the server it was
# meant to run could not start.

# --- Web service ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9        # multipart/form-data uploads

# --- Separation ---
# Demucs 4.x is the current line and provides htdemucs / htdemucs_6s.
demucs>=4.0.1,<5.0
torch>=2.1,<3.0
torchaudio>=2.1,<3.0

# numpy 2.x is not yet compatible with every Demucs release.
numpy>=1.24,<2.0

# FFmpeg itself is a system dependency, not a Python one:
#   macOS          brew install ffmpeg
#   Debian/Ubuntu  sudo apt install ffmpeg
#   Windows        https://ffmpeg.org/download.html, then add it to PATH
yt-dlp
